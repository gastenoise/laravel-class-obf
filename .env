# ---------------------------------------
# CLASS OBFUSCATOR — CORE CONFIG
# ---------------------------------------

# Secret to generate deterministic tokens.
# Must be long, random, and different for each project.
# Required: Do not leave empty.
CLASS_OBF_SECRET=changeme_generate_a_real_secret

# Operating mode:
# dev → no obfuscation, dev-friendly helpers
# production → generates obfuscated mapping and mirroring
CLASS_OBF_MODE=dev

# Length of generated token (8–16 recommended)
CLASS_OBF_TOKEN_LENGTH=8


# ---------------------------------------
# PUBLISHING / DEBUG
# ---------------------------------------

# If true, exposes mapping.json/mapping.js in public/build-obf
# Only useful in debug or analysis environments
CLASS_OBF_PUBLISH_MAPPING=false

# Internal debug logs
CLASS_OBF_LOG=false

# Dump of unmapped classes (dev only)
CLASS_OBF_DUMP_UNMAPPED=false
